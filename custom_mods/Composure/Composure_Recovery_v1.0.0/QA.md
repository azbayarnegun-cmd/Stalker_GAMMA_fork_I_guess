# Composure Recovery v1.0.0 QA

Automated QA covers package layout, Framework API requirements, callbacks,
flat default balance, target limiting, recovery-lock handling, combat detection,
Pass-Out blocking, MCM localization, diagnostics and LuaJIT-safe syntax patterns.

In-engine testing is still required for callback order, the exact combat-table
lifetime, MCM rendering and scheduler timing.
